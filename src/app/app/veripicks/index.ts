/**
 * VeriPicks Application Exports
 *
 * Central exports for VeriPicks application components and services
 */

// Re-export pages for easier imports
export { default as VeriPicksPage } from "./page";

// TODO: Remove local types once package types are properly configured
// All types should be imported from @captify/veripicks package instead
